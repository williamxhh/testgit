use:
	git init   ---to init a git directory

	git status  --- to show the current status of version lib
	
	git add .    ---add current directory to git

	git commit -a -m "**"   ---commit

	git branch   --- list all branches

	git branch test   --- create a new brance names test


